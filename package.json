{
    "scripts": {
        "dev": "nw dist/ --remote-debugging-port=9222",
        "prod": "nwbuild --platforms win32,win64,osx64,linux32,linux64 --buildDir build/ dist/",
        "prod-linux64": "nwbuild --platforms linux64 --buildDir build/ dist/"
    },
    "devDependencies": {
        "nw": "^0.20.0-rc1",
        "nw-builder": "^3.1.3"
    }
}
